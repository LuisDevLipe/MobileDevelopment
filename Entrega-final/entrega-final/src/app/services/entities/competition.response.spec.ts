import { CompetitionResponse } from './competition.response';

describe('CompetitionResponse', () => {
  it('should create an instance', () => {
    expect(new CompetitionResponse()).toBeTruthy();
  });
});
