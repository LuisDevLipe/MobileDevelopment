import { CompetitionRequest } from './competition.request';

describe('CompetitionRequest', () => {
  it('should create an instance', () => {
    expect(new CompetitionRequest()).toBeTruthy();
  });
});
