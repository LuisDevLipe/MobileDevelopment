import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.g2l2.futdata',
  appName: 'FutData',
  webDir: 'www',
  loggingBehavior:'debug',
};

export default config;
